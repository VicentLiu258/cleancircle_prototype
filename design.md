# Clean Circle 移动端产品设计规范

> 文档类型：产品体验与 UI Design System 单一事实源  
> 适用范围：Clean Circle iOS 移动端 MVP，S01–S31 及其状态变体  
> 设计方向：Jo 教练的周期训练手册  
> 版本：v1.0  
> 更新日期：2026-08-07  
> 状态：设计基线，可进入锚点屏评审

---

## 1. 文档目的

本文件定义 Clean Circle 移动端的产品体验、视觉系统、组件行为和页面规范。它是设计、产品、研发以及 GPT / Stitch 等生成工具共同遵循的上游约束。

优先级如下：

```text
已确认业务规则
  > 本 design.md
  > 已批准的组件与参考 Frame
  > 单屏 Screen Contract
  > Stitch / GPT 执行提示词
  > 氛围和风格形容词
```

发生冲突时，禁止让生成工具自行取舍；应回到产品事实和本文件修订。

本文件不承担以下职责：

- 不代替正式品牌 VI、Logo 源文件和字体授权文件。
- 不代替医疗、隐私、支付和应用商店合规审查。
- 不确认仍标注为“待业务确认”的套餐、积分与老用户权益数值。
- 不允许根据展示效果擅自新增功能。

---

## 2. 产品定义

### 2.1 一句话定位

Clean Circle 是一款结合周期阶段、每日身体反馈与运动内容，为女性安排和动态调整日常训练的移动应用。

### 2.2 用户价值

用户打开应用后应快速知道：

1. 今天处于什么周期阶段。
2. 今天建议练什么。
3. 为什么这样安排。
4. 身体状态变化后可以如何调整。
5. 训练完成后记录了什么。

### 2.3 核心体验闭环

```mermaid
flowchart LR
    A["登录与授权"] --> B["周期与运动评测"]
    B --> C["生成今日起 30 天课表"]
    C --> D["每日 Check-in"]
    D --> E["确认或调整今日课程"]
    E --> F["跟练"]
    F --> G["反馈与打卡"]
    G --> H["日历、能量值与连续记录"]
    H --> D
```

### 2.4 MVP 范围

- iOS 优先，Light Mode。
- 7 天免费体验作为当前原型基线。
- 评测后直接生成今日起滚动 30 天课表，不生成长篇专属报告。
- 5 个主 Tab：今日、课程库、日历、社区、我的。
- 社区 MVP 只展示官方内容，不做完整 UGC。
- 支持老用户资产迁移、订阅、买断课程保留、每日调整、投屏与能量值记录。
- S07、S18 当前无定义，不生成页面。

---

## 3. 用户与使用场景

### 3.1 目标用户

- 22–40 岁女性。
- 对周期、状态和运动之间的关系有兴趣，但不希望阅读复杂医学报告。
- 需要“今天具体做什么”，而非泛泛的健康知识。
- 可能在家、健身房、办公室或出差环境中使用。
- 可能处于疲劳、疼痛、情绪波动或注意力较低的状态。

### 3.2 关键使用情境

| 情境 | 用户状态 | 设计响应 |
|---|---|---|
| 早晨快速查看 | 时间短、单手操作 | 3 秒内识别阶段、今日课和主 CTA |
| 训练前 Check-in | 需要表达身体状态 | 选项短、可撤销，不自动替用户做高影响决定 |
| 疲劳或经期变化 | 安全敏感、容易误触 | 解释影响、二次确认、操作留痕、允许撤销 |
| 跟练中 | 注意力在动作而非手机 | 控件少、触控大、续播明确 |
| 训练后 | 疲劳、希望快速完成 | 反馈可跳过，奖励解释清楚 |
| 体验到期 | 可能担忧历史数据丢失 | 清晰区分锁定内容与永久保留资产 |
| 老用户迁移 | 对资产安全敏感 | 分类展示、批次可追溯、失败项可单独重试 |

---

## 4. 体验原则

### 4.1 具体，不玄学

周期是产品输入，不是神秘装饰。建议必须说明依据，例如“黄体期 + 睡眠一般”，不能只写“听从身体的声音”。

### 4.2 一屏一件主要任务

每个页面只能有一个视觉主任务和一个主要 CTA。其他内容通过层级、滚动或次级入口延后。

### 4.3 用户始终有控制权

降级、重排、订阅、迁移、删除数据等操作必须解释后果。高影响操作提供取消或撤销路径。

### 4.4 真实胜过“高级感”

优先使用真实教练、真实动作、真实空间和真实产品数据。没有素材时使用明确占位，不让模型生成假人物、假评价和假统计。

### 4.5 克制但不冷漠

界面以系统化信息表达为主；温度来自 Jo 的具体解释和真实影像，而不是渐变、光晕、月亮、花朵或空泛鸡汤。

### 4.6 状态必须连续

同一页面的未开始、进行中、已完成、锁定等状态必须继承相同组件几何，只改变必要数据、文案、显隐和操作状态。

---

## 5. 设计概念：Jo 教练的周期训练手册

### 5.1 设计人格

Clean Circle 像一位有经验的女教练每天在训练手册上写下观察与建议。视觉应兼具训练记录的秩序感、生活方式内容的温度和健康产品的可信度。

不是以下方向：

- 神秘月相、占星或灵性疗愈产品。
- 粉色化、幼态化的“女生专属”产品。
- 医院、检查报告或冷蓝色医疗仪表盘。
- 高饱和、倒计时、强销售导向的健身商城。
- 由圆角卡片、渐变光晕和抽象插画拼接的 AI 模板。

### 5.2 项目识别件

| 识别件 | 含义 | 使用位置 | 限制 |
|---|---|---|---|
| Cycle Ruler 周期尺 | 28/30 天阶段、今天位置、预测范围 | 今日、日历、课表总览 | 不做装饰性大月亮 |
| DAY 标记 | 当前阶段中的日期信息 | 今日、课程详情、完成海报 | 只表达真实 Day 数据 |
| Jo's Note | 教练观察与建议 | 今日、推荐理由、关键确认 | 最多 2 行，观察 → 建议 |
| 真实训练影像 | 人与内容的可信度 | 今日课程、课程库、课程详情 | 只用批准素材 |
| 完成印记 | 完课与连续记录 | S13、日历、打卡记录 | 不用烟花、奖杯、彩带 |

每屏最多选择一个识别件作为视觉主角，其他识别件降为信息层。

---

## 6. 信息架构

### 6.1 主导航

| Tab | 核心任务 | 默认入口 |
|---|---|---|
| 今日 | 看状态、Check-in、开始或调整今日训练 | S09 |
| 课程库 | 搜索、筛选、查看公开/买断/自选课程 | S29 |
| 日历 | 查看周期、课表、完成状态与单日课程 | S25 |
| 社区 | 阅读 Jo 与官方内容 | S31 |
| 我的 | 管理权益、资产、记录、隐私与设置 | S26 |

### 6.2 页面层级

- 一级页：5 个 Tab 页面。
- 二级页：课程详情、资料、记录、订阅管理、设置等。
- 模态层：Check-in、降级、经期首日确认、加入日历、迁移确认。
- 全屏沉浸层：视频播放器、品牌欢迎、生成中、分享海报。

### 6.3 导航规则

- 一级页保留标准全宽 iOS Tab Bar。
- 二级页隐藏 Tab Bar，使用标准返回导航。
- Modal 不承担深层浏览；超过一个主要选择流程时进入二级页。
- 页面标题不得在导航栏和正文首行重复出现。
- 关闭付费墙后必须回到可用的基础页，不阻断买断内容。

---

## 7. 画板、栅格与安全区

### 7.1 基准设备

| 项目 | 规范 |
|---|---|
| 基准画板 | iPhone 15 Pro，393 × 852 pt |
| 次级校验 | 375 × 812 pt |
| 方向 | 竖屏 |
| 模式 | Light Mode |
| 主栅格 | 8 pt |
| 微调单位 | 4 pt |
| 页面左右边距 | 20 pt |
| 紧凑列表左右边距 | 16–20 pt |
| 最小点击区 | 44 × 44 pt |

### 7.2 垂直节奏

- 页面导航区到第一段内容：16–24 pt。
- 页面大区块间距：24–32 pt。
- 同组内容间距：8–16 pt。
- 标签与正文间距：4–8 pt。
- 固定底部操作区与内容之间保留滚动尾部间距，禁止内容被 Home Indicator 遮挡。

### 7.3 首屏预算

首个 852 pt 视口内：

- 最多 2 个实体卡片或面板。
- 最多 1 个主要 CTA、2 个次级动作。
- 最多 1 张内容图片。
- 最多 4 个短标签；更多标签转为列表或横向筛选。
- Tab 页面必须在不滚动时看见主任务和主 CTA。
- 不为填满留白增加无意义装饰。

---

## 8. 色彩系统

### 8.1 品牌基色

| Token | 色值 | 名称 | v2 用途 |
|---|---|---|---|
| `brand-rose` | `#aa6459` | 干枯玫瑰 | 品牌标记、选中态、进度、重点数字 |
| `rose-deep` | `#68403e` | 深玫瑰 | 深色阶段、严肃操作、深色辅助面 |
| `rose-mid` | `#824d48` | 中玫瑰 | 次级强调、警告标记、排卵期 |
| `rose-light` | `#ba7872` | 浅玫瑰 | 轻量选中、卵泡期、浅强调 |
| `cocoa` | `#49352e` | 可可棕 | 主文字、关键图标、可访问主按钮 |
| `oat` | `#f3efe8` | 燕麦白 | 页面背景、浅色正文反白 |

### 8.2 语义映射

本 v2 对现有提示词的语义映射做以下覆盖：

| 语义 | 规范 |
|---|---|
| 页面背景 | `oat` |
| 主文字 | `cocoa` 100% |
| 次级文字 | `cocoa` 68% |
| 弱文字/占位 | `cocoa` 48% |
| 分隔线 | `cocoa` 12% |
| 浅选中背景 | 品牌基色 8–12% 透明度 |
| 主按钮 | `cocoa` 底 + `oat` 字 |
| 品牌强调 | `brand-rose` |
| 破坏性 | `rose-deep` + 明确图标/文字 |
| 成功 | `brand-rose` + 对勾 + 成功文案 |
| 警告 | `rose-mid` + 警告图标 + 解释文案 |

只允许基于六个品牌色创建透明度层级；禁止新增纯白、纯黑、冷灰、绿色、琥珀色和紫色 hex。

### 8.3 对比度

- `cocoa` / `oat` 对比度约 10:1，可用于正文和主按钮。
- `brand-rose` / `oat` 约 3.92:1，不作为普通字号反白按钮的唯一组合。
- 颜色不得成为状态的唯一表达方式。
- 对比度验收以实际字号、字重和渲染背景为准。

### 8.4 周期阶段编码

| 阶段 | 基色 | 文字缩写 | 周期尺位置 | 辅助形状 |
|---|---|---|---|---|
| 月经期 | `brand-rose` | 经 | 第 1 段 | 实心短线 |
| 卵泡期 | `rose-light` | 卵泡 | 第 2 段 | 左开口短线 |
| 排卵期 | `rose-mid` | 排 | 第 3 段 | 中心点 |
| 黄体期 | `rose-deep` | 黄体 | 第 4 段 | 右开口短线 |

所有日历、图例和周期尺同时显示阶段文字或可理解符号。状态标记与阶段色分层表达，例如日期底色表示阶段，日期角标表示完成状态。

---

## 9. 字体与排版

### 9.1 字体栈

- UI、正文与数字：PingFang SC、SF Pro、系统无衬线。
- 品牌时刻：Source Han Serif SC 或经授权的中文宋体。
- 宋体只用于：S03 欢迎、S13 分享海报、S22 付费墙价值标题。
- 未确认字体授权前不得在交付稿中使用 Canela、Freight 等不可控替代字体。

### 9.2 字级

| Token | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| `display` | 32/40 | Medium | 品牌时刻，最多 2 行 |
| `title` | 24/32 | Semibold | 页面标题 |
| `section` | 18/26 | Semibold | 区块标题、主要卡片标题 |
| `body` | 16/24 | Regular | 正文、列表主要文字 |
| `body-small` | 14/20 | Regular | 次级信息、辅助说明 |
| `caption` | 12/18 | Medium | 时间、标签、法律提示 |
| `metric` | 28/34 | Semibold | Day、余额、时长等真实数字 |

### 9.3 排版规则

- 中文页面标题建议不超过 14 个汉字。
- 按钮建议不超过 8 个汉字。
- 一个正文块建议不超过 42 个汉字；更长内容拆段或进入详情页。
- 左对齐是功能页默认方式；品牌页和结果海报可居中。
- 英文只保留品牌已确认短句，不添加 START HERE、PREMIUM、WELLNESS 等装饰词。
- 不用斜体承载关键中文信息。

---

## 10. 形状、边框与层级

### 10.1 圆角

| Token | 数值 | 用途 |
|---|---|---|
| `radius-sm` | 8 pt | 小控件、状态标签、日历单元 |
| `radius-md` | 12 pt | 输入、按钮、列表容器 |
| `radius-lg` | 16 pt | 主内容面板、课程图片 |
| `radius-sheet` | 24 pt | 底部弹层外层顶部 |

禁止全局使用 20–28 pt 大圆角。Pill 只用于短状态和筛选。

### 10.2 边框

- 默认 1 pt，使用 `cocoa` 12–20% 透明度。
- 选中态可使用 1.5–2 pt `brand-rose` 描边。
- 破坏性操作不依赖加粗红框；使用图标、文案和确认层表达。

### 10.3 阴影

- 普通卡片默认无阴影。
- Modal / Sheet 可使用 `0 12 40`、`cocoa` 10% 透明度。
- 禁止多层柔光、彩色阴影和悬浮感堆叠。

### 10.4 分组

优先级：

```text
留白 > 标题层级 > 分隔线 > 浅色面 > 实体卡片 > 阴影
```

禁止卡片嵌套卡片，禁止每段文字单独包裹容器。

---

## 11. 图标、摄影与插画

### 11.1 图标

- 使用同一套圆角线性图标，线宽 1.75 pt。
- 激活态可填充或提高字重，不更换图标风格。
- 图标必须表达功能，不承担装饰。
- 关键状态同时显示图标和文字。

### 11.2 摄影

照片方向：

- 真实 Jo 或批准教练。
- 真实动作、真实器械、真实居家或训练空间。
- 自然侧光或环境光，允许轻微颗粒和生活痕迹。
- 构图优先展示动作和身体姿态，不做美妆式居中人像。
- 肤色自然，不过度磨皮，不制造不真实身材。

课程图片比例：

- 今日主课程：4:3。
- 课程列表：4:3 或 3:2，同一列表必须一致。
- 课程详情 Hero：4:3，不超过首屏 42% 高度。
- 分享海报：9:16 独立画布。

### 11.3 无素材时

使用明确占位：

```text
4:3 IMAGE PLACEHOLDER
Use approved Jo photo: luteal low-intensity yoga
```

不得由 GPT/Stitch 生成合成人物充当品牌资产。

### 11.4 禁止的装饰

- 渐变背景、模糊色块、发光、玻璃拟态。
- 漂浮球体、星光、闪粉、3D 奖杯。
- 通用瑜伽剪影、叶片、花朵、无意义波浪。
- 每个页面或空态都重复月亮/月相。

---

## 12. 核心组件

### 12.1 导航栏

| 属性 | 规范 |
|---|---|
| 高度 | 遵循 iOS 标准导航与状态栏 |
| 左侧 | 返回或页面上下文 |
| 中部 | 标题仅在必要时显示 |
| 右侧 | 最多 2 个图标动作 |
| 背景 | `oat`，滚动时可出现 1 pt 分隔线 |

### 12.2 Tab Bar

- 标准全宽 iOS Tab Bar，不做悬浮胶囊。
- 5 个 Tab 均显示图标与中文标签。
- 激活态：`brand-rose` 图标/短线 + Medium 标签。
- 非激活态：`cocoa` 60–68%。
- 必须包含 Home Indicator 安全区。

### 12.3 按钮

| 类型 | 高度 | 样式 | 使用规则 |
|---|---|---|---|
| Primary | 52 pt | `cocoa` 底、`oat` 字、12 pt 圆角 | 每屏最多 1 个 |
| Secondary | 48–52 pt | 透明底、1 pt 描边 | 与主操作同组 |
| Tertiary | ≥44 pt 点击区 | 文字 + 可选箭头 | 低优先级 |
| Destructive | 48–52 pt | `rose-deep` 文字或描边 | 必须说明后果 |
| Icon button | 44 × 44 pt | 无实体底或轻量选中 | 导航与工具 |

Loading 状态保留按钮宽度和原标签语义，例如“登录中…”，禁止页面布局跳动。

### 12.4 输入与选择

- 输入框高度 52 pt，12 pt 圆角。
- 标签不得只依靠 placeholder；聚焦后仍能理解字段含义。
- 错误状态同时包含错误文字和图标，不只改变颜色。
- 单选列表优先使用标准行 + radio/check，不将每项设计成巨型卡片。
- 多选超过 5 项时使用纵向列表，不做多行 pill 墙。
- 日期优先使用平台原生 Picker 或 Calendar。

### 12.5 Cycle Ruler

结构：

1. DAY 数字或日期。
2. 当前阶段文字。
3. 28/30 天水平细线。
4. 四阶段分段与图例。
5. 今天位置标记。
6. 可选预测范围，不输出医学确定语气。

高度建议 72–96 pt。默认扁平布局，不包大卡片。

### 12.6 Check-in 摘要行

- 未完成：标题 + 四维摘要 + “去记录”。
- 已完成：能量、情绪、睡眠、不适的紧凑文字摘要。
- 点击进入 S19 Sheet。
- 默认使用扁平列表行和分隔线，不作为第二张 Hero 卡。

### 12.7 今日课程面板

包含：

- 批准的 4:3 图片或占位。
- 状态：未开始 / 进行中 / 已完成 / 已降级 / 锁定 / 兜底。
- 课程名。
- 时长、强度、部位等最多 3 项元信息。
- Jo's Note 或推荐依据。
- 一个主动作。

禁止同时出现大量 badge、kcal、评分、人数、倒计时和多个 CTA。

### 12.8 Jo's Note

固定结构：

```text
观察：用户可验证的数据或状态。
建议：具体调整和可选择动作。
```

- 最多 2 行正文，必要时进入“查看原因”。
- 不使用聊天气泡、头像和引号，避免伪社交感。
- 不给出诊断、治疗或保证效果的表述。

### 12.9 标签与 Chip

- 高度 28–32 pt。
- 只承载短状态、阶段或筛选。
- 单个标签建议不超过 5 个汉字。
- 同屏同一行最多 4 个。
- “来例假了”“今天太累”等重要行为不得伪装成装饰 Chip。

### 12.10 日历单元

一个日期单元需要分离表达：

- 日期数字。
- 周期阶段背景/短线。
- 今日边框。
- 完成、部分完成、休息、未来、锁定状态角标。

完成符号：`✓`；部分完成：`◐`；休息：短横线；锁定：小锁 + 文字图例。禁止只用四个相近色块表达全部信息。

### 12.11 Bottom Sheet

- 顶部圆角 24 pt，有 Home Indicator 安全区。
- 内容左右边距 20 pt。
- 标题 + 事实/选择 + 操作，不在 Sheet 中继续多层导航。
- 主动作在上、取消/保持在下，敏感操作根据平台习惯复核顺序。
- 高度超过屏幕 80% 时改为全屏二级页。

### 12.12 Dialog

- 只用于不可逆、高影响或需要二次确认的行为。
- 标题明确动作，正文说明影响范围。
- 不使用“确定吗？”这种缺乏上下文的标题。
- 迁移绑定、注销账号、删除健康数据必须使用明确二次确认。

### 12.13 Toast

- 仅反馈已完成的轻量操作。
- 自动消失前不承载必须阅读的合规信息。
- 可撤销操作需要保留足够时长和“撤销”按钮。
- 成功、信息、错误同时使用图标和文字。

### 12.14 空态、错误与 Loading

- 空态结构：图标可选 + 事实标题 + 下一步 + 最多一个动作。
- 不默认生成插画。
- Loading 优先局部骨架或按钮状态，避免无必要的整屏等待仪式。
- 错误说明发生了什么、数据是否安全、用户能做什么。

---

## 13. 页面族模板

| 页面族 | 屏幕 | 固定骨架 | 视觉重点 |
|---|---|---|---|
| 品牌入口 | S01、S03、S06、S13 | 单焦点 + 单主动作 | 品牌/完成时刻 |
| 问卷表单 | S04、S05、S28 | 进度/标题 + 输入 + 底部动作 | 清晰与可信 |
| 法务阅读 | S02 | 标题 + 正文 + 返回 | 三份协议独立查阅 |
| 今日任务 | S08、S09、S10、S12 | 周期 + 主任务 + 原因 | 开始训练 |
| 全屏训练 | S11 | 视频 + 最少控制 | 动作内容 |
| 日历数据 | S25、S27 | 数据区 + 扁平明细 | 状态连续性 |
| 资产设置 | S14–S17、S24、S26 | 分组列表 + 状态摘要 | 信任与可追溯 |
| 商业转化 | S22、S23 | 价值证据 + 方案 + 法务 | 透明订阅 |
| 探索内容 | S29–S31 | 搜索/筛选 + 内容列表 | 发现内容 |
| 底部弹层 | S19–S21、S30 | 标题 + 事实 + 选择 | 快速决策 |

每个页面族先批准一个参考 Frame。其他页面只能复用该 Frame 的栅格、字体、按钮、行高和容器逻辑。

---

## 14. 屏幕规范总表

### 14.1 新用户主链路

| ID | 页面 | 用户目标 | 主内容 | 主动作 | 关键状态 |
|---|---|---|---|---|---|
| S01 | 启动与登录 | 安全进入应用 | 品牌、微信/手机号/Apple、协议勾选与三项独立链接 | 微信一键登录（勾选后进 S03 / 旧账号进 S14） | 默认、验证码错误、未同意、Loading、旧账号匹配 |
| S02 | 协议全文查看 | 按需查阅协议 | 对应协议正文 | 返回登录页 | 用户协议、隐私政策、健康数据处理说明 |
| S03 | 专属欢迎 | 建立品牌关系 | 品牌影像、昵称、价值承诺 | 开始评测 | 默认 |
| S04 | 问卷 | 提供排课信息 | 进度、题目、选项 | 下一题/完成评测 | 单选、多选、日期、数字、滑杆、跳题、设备、敏感声明 |
| S05 | 提交确认 | 确认数据用途 | 生成内容摘要 | 生成我的课表 | 默认 |
| S06 | 生成中 | 理解等待原因 | 进度、处理说明 | 自动进入课表 | Loading、失败 |
| S08 | 本月课表 | 理解 30 天安排 | Cycle Ruler、课表预览 | 查看今天 | 默认 |
| S09 | 今日首页 | 开始或调整今日训练 | 阶段、Check-in、课程、原因 | 开始/继续训练 | 未 Check-in、未开始、进行中、已完成、降级、临期、锁定、兜底、评测未完成 |
| S10 | 课程详情 | 判断课程是否适合 | 图片、元信息、推荐理由、注意事项 | 开始跟练 | Loading、周期定制、自选、锁定 |
| S11 | 视频与投屏 | 完成跟练 | 视频、进度、核心控制 | 我已完成 | 播放、续播、设备搜索、遥控、无设备、失败 |
| S12 | 完课反馈 | 快速反馈训练感受 | 强度、身体、情绪 | 完成打卡 | 默认、双主课部分完成 |
| S13 | 打卡成功 | 确认记录与奖励 | 完成印记、能量、连胜 | 返回今日 | 默认、7 天里程碑 |

### 14.2 老用户迁移与每日调整

| ID | 页面 | 用户目标 | 主内容 | 主动作 | 关键状态 |
|---|---|---|---|---|---|
| S14 | 发现历史资产 | 确认匹配账号 | 手机号、打卡、能量、买断课摘要 | 查看并迁移 | 默认 |
| S15 | 迁移确认 | 核对迁移内容与规则 | 三类资产、绑定规则 | 确认迁移 | 默认、二次确认、迁移中 |
| S16 | 迁移结果 | 确认每类资产结果 | 分类结果、批次号 | 查看我的资产 | 成功、部分成功、失败、迁移中 |
| S17 | 我的资产 | 查看永久资产 | 能量、历史打卡、买断课 | 查看课程/完成评测 | 默认、无买断课 |
| S19 | 每日 Check-in | 记录身体状态 | 能量、情绪、睡眠、不适 | 提交 | 填写、建议降级、无需调整 |
| S20 | 今天太累 | 主动降低今日强度 | 原课与新课比较 | 确认替换 | 默认、替换后可撤销 |
| S21 | 来例假了 | 更新经期首日并重排 | 影响说明、当前/新阶段 | 确认更新 | 确认、重排中、完成、撤销 |

### 14.3 商业化与日常页面

| ID | 页面 | 用户目标 | 主内容 | 主动作 | 关键状态 |
|---|---|---|---|---|---|
| S22 | 订阅付费墙 | 理解价值与价格 | 已获得价值、权益、套餐、法务 | 立即订阅 | 新用户、老用户；可关闭 |
| S23 | 支付结果 | 确认支付状态 | 状态、权益时间、下一步 | 返回今日/重试 | 成功、失败、结果未知 |
| S24 | 订阅管理 | 查看或管理续费 | 套餐、扣费时间、权益 | 取消/重新订阅 | 生效、已取消、已失效 |
| S25 | 日历 | 查看阶段和训练状态 | 月历、图例、单日课程 | 开始课程/加练 | 默认、部分完成、未来锁定、单日展开 |
| S26 | 我的 | 管理个人资产与设置 | 会员、能量、工具、设置 | 按入口进入 | 体验中、订阅中、失效 |
| S27 | 能量值 | 查看余额和明细 | 余额、流水、兑换入口 | 查看兑换/返回训练 | 默认、空明细 |
| S28 | 周期资料 | 修改周期基础资料 | 日期、周期长度、规律性 | 保存 | 编辑、保存确认、删除健康数据入口 |
| S29 | 课程库 | 发现和筛选课程 | 搜索、筛选、课程列表 | 查看课程 | 默认、搜索结果、空结果、直播中 |
| S30 | 加入日历 | 为自选课选择日期 | 课程、未来 28 天日期 | 确认加入 | 选择、成功 |
| S31 | 社区 Homies | 阅读官方内容 | 官方说明、Jo 内容流 | 查看文章 | MVP 官方内容 |

### 14.4 未单独编号的二级页

- 迁移记录。
- 打卡记录。
- 设置、隐私与注销。
- 注销账号二次确认。
- 删除健康数据申请。
- 消息中心。
- 邀请好友占位。
- 帮助中心与客服。
- Ritual 食谱/内容详情。

这些页面复用资产设置或探索内容页面族，不另起视觉方向。

---

## 15. 关键页面布局

### 15.1 S09 今日首页

首屏顺序：

1. 问候、日期、消息入口。
2. DAY 标记 + Cycle Ruler + 阶段与预测。
3. Check-in 紧凑摘要行。
4. 唯一主课程面板。
5. Jo's Note。
6. 主 CTA。
7. 一个安静的调整入口。
8. Tab Bar。

折叠下方：饮食建议、Ritual 和更多内容。禁止把三张 Ritual 卡、订阅条、能量值、连胜与所有快捷操作同时塞进首屏。

状态变化只发生在课程面板和必要的顶部提示：

- 未 Check-in：摘要行显示“去记录”。
- 进行中：显示进度与“继续训练”，隐藏降级入口。
- 已完成：显示实际时长与能量到账，不继续突出主 CTA。
- 已降级：显示新课和原课留痕，提供撤销。
- 锁定：历史和买断入口保持可用。
- 兜底：说明“安全兜底课”及原因，不留空白。

#### 15.1.1 资源位三槽位（Work / Fuel / Care / Commerce）

S09「今日」页的可运营内容以**资源位**形式组织（区别于后台「开屏/Banner 资源位投放」）：

| 槽位 | 用户可见内容 | 首屏/折叠 | 内容来源 | 后台配置 |
|------|-------------|----------|---------|---------|
| **Work** | 今日主课程面板 + 主 CTA | 首屏 | 排课引擎 D03 | B55 组件排序（主面板不可移除） |
| **Fuel** | 饮食营养建议 | 折叠下方 | 建议库 B14 阶段话术 | B55 + B14 |
| **Care** | Ritual / 音视频关怀内容 | 折叠下方 | Ritual 内容库 | B55 + 内容配置 |
| **Commerce** | Jo 姐好物 / 会员 Banner | 折叠下方 | B41 商品入口 | B55 + B41 |

规则：

- 首屏仅 Work 一个实体面板；Fuel / Care / Commerce 放在「今天还可以做」折叠区。
- Fuel / Care 后期可插入商品 SKU（占位，MVP 不启用）。
- `GET /today` API 聚合下发各槽位内容与展示规则。
- 体验进度条（trial-strip）不属于资源位，独立位于首屏顶部，跳转 S22。

### 15.2 S25 日历

- 上部 40–48%：30 天滚动月历、阶段图例、训练状态图例。
- 下部：选中日期的课程明细、加练入口、场景化销售占位。
- 用户选择日期后在同页更新下部详情，不立即跳新页面。
- 历史日期不提供补打入口。

#### 15.2.1 日历格子三层编码

每个日期格分层表达，避免仅靠颜色传递信息：

| 层级 | 位置 | 编码 | 含义 |
|------|------|------|------|
| 第 1 层 | 背景色 | 品牌玫瑰色阶四阶段 | 生理周期预测（月经期 `#aa6459` / 卵泡期 `#ba7872` / 排卵期 `#824d48` / 黄体期 `#68403e`） |
| 第 2 层 | 右上角 | `✓` / `◐` / 空 | 课程完成状态（B-04：全完成 / 部分完成 / 未完成） |
| 第 3 层 | 底部居中 + 左下角 | 主图标 + 次要角标 | 训练意图与叠加状态 |

**训练意图主图标（底部居中）：**

| 图标 | 含义 |
|------|------|
| `↑` | Push — 可上强度 |
| `●` | Warm — 中等负荷 |
| `～` | Soft — 低冲击 / 恢复向 |
| `□` | 休息日 |
| `!` | 兜底课（B-09，amber 描边叠加） |

**次要角标（左下角，最多 1 个，按优先级）：** `🔒` 失效锁定 > `↺` Re-plan 变更 > `◇` 含自选加练

**今日标记：** 外圈 ring（Cocoa Brown `#49352e`），不使用额外图标。

#### 15.2.2 当日课表与入口

- 月度回顾卡片：非惩罚式统计（如「本月已完成 8 次 · 160 分钟」），不显示中断归零的 streak。
- 当日课程列表：定制主课 + 自选加练（可删除撤销）。
- **加练入口**：`+ 加练` → 课程库 S29；副文案说明仅计时长与能量，不计打卡连胜（B-04）。
- **场景化销售占位**（后期可配）：Commerce 资源位，根据训练/恢复/周期状态推荐 SKU（如恢复类冲剂），跳转 S32 或第三方平台；来源 B41/B55。

### 15.3 S04 问卷

- 顶部：返回、进度与“第 N / 12 题”。
- 中部：问题和单一题型。
- 底部：主要下一步，次级返回或跳过。
- 选项使用标准列表；只有需要大面积图形判断的题目才使用选择卡片。
- 敏感健康选项出现说明，但不打断为多层弹窗。
- 已填写答案返回时保留。

### 15.4 S22 付费墙

- 可关闭。
- 先展示用户已体验的具体价值，再展示权益和套餐。
- 只突出当前可售套餐；未确认套餐不伪装成可购买项。
- 自动续费、取消方式、恢复购买和协议始终可见。
- 不使用倒计时、虚假折扣、默认隐藏关闭、误导性主次按钮。

---

## 16. 业务状态与交互规则

### 16.1 免费体验与锁定

- 当前基线为 7 天体验。
- 体验结束后的定制未来课表、每日建议和未完成定制内容锁定。
- 历史打卡、历史数据和买断课程永久可见。
- 付费墙触发后可关闭。
- 今日页允许保留轻量续订入口，不阻断基础浏览。

### 16.2 完课判定

- 播放进度达到可配置阈值，当前原型为 80%，可自动判定完成。
- 用户也可以手动选择“我已完成”。
- 完成写入后状态不回退，避免奖励与打卡重复。

### 16.3 当日多课与打卡

- 1 节主课：完成即计当日打卡。
- 2 节主课：全部完成才计当日打卡。
- 只完成 1 节显示“部分完成 ◐”。
- 当日 24:00 前可继续，历史日期不补打。
- 自选加练只计时长和能量，不计连续打卡。

### 16.4 Check-in 降级

- 低能量、情绪不佳或睡眠差时，系统只提出建议。
- 替换前展示原因、原课和新课。
- 用户确认后才替换，仅当日有效。
- 替换后保留原课记录并允许撤销。

### 16.5 经期首日更新

- “来例假了”属于高影响敏感操作。
- 二次确认后立即重算当前阶段和今日起未来课表。
- 已完成历史不覆盖。
- 支持撤销，撤销后按原预测重新计算。

### 16.6 排课兜底

- 无匹配课程时使用运营配置的低强度安全兜底课，或明确建议休息。
- 前端显示兜底标识和解释。
- 异常上报后台，页面不得空白。

### 16.7 资产迁移

- 一个原小程序账号只绑定一个 App 账号。
- 打卡、能量值、买断课三类分别展示结果。
- 部分失败允许单独重试。
- 批次号、时间、来源和结果永久可追溯。

### 16.8 能量值

当前原型占位：

- 主课完成 +20。
- 加练 +10。
- 连续 7 天额外 +50。

所有变动必须显示业务类型、关联内容、时间和变动后余额。正式数值待运营确认。

---

## 17. 文案系统

### 17.1 语气

Jo 的语气应当专业、直接、温和，但不哄劝和不夸张。优先使用事实、判断和选择。

```text
事实：昨晚睡眠偏少，今早能量偏低。
判断：今天不适合继续中强度核心训练。
选择：可以换成 10 分钟拉伸，也可以保留原课。
```

### 17.2 推荐用语

| 场景 | 推荐 |
|---|---|
| 个性化内容 | 为你安排、周期定制、今日建议 |
| 系统调整 | 已调整、建议替换、保持原课、撤销 |
| 身体状态 | 能量偏低、睡眠一般、轻微不适 |
| 订阅锁定 | 定制内容已锁定；历史记录仍可查看 |
| 数据处理 | 用于生成课表；可在隐私设置中删除 |

### 17.3 避免用语

- “AI 定制”“AI 为你赋能”。
- “遇见更好的自己”“开启专属蜕变旅程”。
- “请允许自己慢下来”“温柔拥抱每一个阶段”等高频疗愈套话。
- “绝对安全”“科学治愈”“改善疾病”等无依据健康承诺。
- 过度重复“专属、解锁、能量、仪式感、懂你”。

### 17.4 错误文案

错误文案回答三个问题：

1. 发生了什么。
2. 数据或权益是否受影响。
3. 用户下一步可以做什么。

示例：

```text
迁移未完成
原小程序资产未发生变化。你可以重试，或凭批次号联系客服。
```

---

## 18. 动效与触觉反馈

### 18.1 动效原则

- 动效解释状态变化，不用于制造“高级感”。
- 页面切换遵循 iOS 平台习惯。
- 常规反馈 160–220 ms；Sheet 220–320 ms；内容骨架不闪烁。
- 支持 Reduce Motion；不得依赖动画才能理解结果。

### 18.2 关键动效

| 场景 | 动效 |
|---|---|
| Check-in 提交 | Sheet 下收，摘要行内容更新 |
| 课程降级 | 原课与新课交叉更新，保留撤销 Toast |
| 经期重排 | 周期尺位置更新，随后更新未来课表；不做大面积粒子效果 |
| 完课 | 完成印记轻量出现，能量值数字更新 |
| 加入日历 | 日期确认后 Sheet 转为成功状态 |
| Loading | 局部骨架、按钮进度或细进度线 |

### 18.3 触觉反馈

- 选择题与日期：轻触反馈。
- 完课、迁移成功、加入日历：成功反馈。
- 删除、注销、绑定不可逆确认：警告反馈。
- 连续快速操作需节制，避免每次滚动/切换都震动。

---

## 19. 可访问性与包容性

### 19.1 视觉

- 正文目标对比度不低于 4.5:1；大字号目标不低于 3:1。
- 不仅用颜色区分周期、错误、完成和锁定。
- 支持动态字体，至少检查系统默认与较大一级。
- 触控区最小 44 pt。
- 焦点顺序与视觉顺序一致。

### 19.2 内容

- 不假设所有用户周期规律。
- 使用“预计、估算、根据你提供的信息”等非确定性表述。
- 不将经期、身体不适或订阅失效写成羞耻或失败。
- 不用“正常/异常”评价用户身体，改用“规律/不规律”“需要关注”等具体语言。

### 19.3 辅助技术

- 图标按钮提供可读标签。
- 日历日期读出日期、阶段和状态，例如“1 月 17 日，黄体期，部分完成”。
- 视频控制提供 VoiceOver 名称、当前值和可执行动作。
- 图片提供与动作内容相关的替代文本，不写“精美图片”。

---

## 20. 隐私、健康与合规体验

- 周期、情绪、睡眠和身体不适属于敏感健康相关数据。
- 登录页展示《用户协议》《隐私政策》《健康数据处理说明》独立入口，用户可按需查阅全文（S02，非必须流程）；勾选同意后即可登录，不强制进入协议全文页。
- 授权必须可选，设备连接和通知权限不得强迫。
- 用户可查看、修改、导出和申请删除健康数据。
- 删除与注销是不同操作，影响范围必须分别解释。
- 订阅页展示价格、周期、自动续费、取消方式、协议和恢复购买。
- 医疗免责声明应在需要的位置出现，不得每屏重复造成恐慌。

---

## 21. AI / Stitch 使用治理

### 21.1 工具角色

- GPT：整理产品事实、生成 Screen Contract、检查一致性与文案。
- Stitch：在已批准的设计系统和参考 Frame 内执行视觉稿。
- 人类设计师：决定信息优先级、品牌识别、组件、可用性、例外和最终验收。

### 21.2 禁止让模型决定的内容

- 品牌方向、字体授权和 Logo 形态。
- 价格、权益、奖励数值和促销方案。
- 医学结论与安全规则。
- 是否新增页面、功能、统计、评价、用户人数。
- 组件是否可以脱离系统产生新样式。

### 21.3 生成顺序

1. 锁定本 design.md。
2. 先生成 S03、S09、S25 三张锚点屏。
3. 从锚点屏提取组件，不单独凭空生成组件库。
4. 为 9 个页面族各批准一个母版。
5. 生成主流程主态。
6. 复制已批准 Frame 生成状态差量。
7. 真机与可访问性验收。

### 21.4 单屏输入格式

```text
Screen ID and state:
Approved page-family reference:
User goal:
Entry and exit:
First viewport order:
Below fold:
Exact Chinese copy:
Real data:
Components to reuse:
New component request:
State delta:
Must not add:
Acceptance checks:
```

### 21.5 AI 模板禁令

- 不使用渐变、玻璃拟态、发光、模糊色块、漂浮球。
- 不生成合成人物、假头像、假评价、假统计、假社交证明。
- 不添加无意义英文、月亮、叶片、花朵、星星和 3D 图标。
- 不为每段内容创建卡片或 pill。
- 不改变已批准组件几何来制造状态差异。
- 未确认内容显示 `[待确认]`，不得自动补造。

详细执行提示词见 `docs/stitch-prompt-system-human-mobile-v2.md`。

---

## 22. 设计与研发交付

### 22.1 Frame 命名

```text
Mobile / {Screen ID} / {Screen Name} / {State}
```

示例：

```text
Mobile / S09 / Today / Checked-NotStarted
Mobile / S09 / Today / InProgress
Mobile / S09 / Today / Completed
```

### 22.2 组件命名

```text
Component / Category / Name / Variant / State
```

示例：

```text
Component / Button / Primary / Default
Component / Workout / TodayPanel / InProgress
Component / Calendar / DayCell / Partial
```

### 22.3 Token 命名

```text
color.brand.rose
color.text.primary
color.border.subtle
space.8
radius.12
type.body.16
motion.standard.200
```

### 22.4 交付标注

每个页面需标注：

- 使用的组件与变体。
- 数据来源和空值行为。
- Loading、Empty、Error、Disabled 和权限状态。
- 点击、返回、关闭、撤销和深链行为。
- 键盘类型、输入限制与校验时机。
- Safe Area、滚动边界与固定区域。
- 埋点事件名称由产品/数据补充。

---

## 23. Definition of Done

### 23.1 产品完整性

- [ ] 页面目标、入口、出口和主动作明确。
- [ ] 业务规则与 `decisions.ts` 一致。
- [ ] 未确认数值没有被模型补造。
- [ ] 主态、Loading、Empty、Error、Disabled 和关键异常态已覆盖。
- [ ] 高影响操作有解释、确认和必要的撤销路径。

### 23.2 视觉一致性

- [ ] 使用统一栅格、字级、圆角、图标和按钮。
- [ ] 首屏不超过 2 个实体面板。
- [ ] 没有卡片嵌套、万物 pill 或浮动 Tab Bar。
- [ ] 没有渐变、光晕、通用月亮和 AI 合成人物。
- [ ] 同一状态族只改变必要内容，不改变组件几何。

### 23.3 内容与品牌

- [ ] 每条个性化建议能说明依据。
- [ ] 文案符合事实 → 判断 → 选择。
- [ ] 没有空泛疗愈、夸张承诺和无意义英文。
- [ ] 至少一个 Clean Circle 识别件被正确使用，但未堆砌。
- [ ] 所有照片来自批准素材或明确占位。

### 23.4 可用性

- [ ] 3 秒内可识别页面主任务。
- [ ] 一个页面只有一个主 CTA。
- [ ] 触控区至少 44 pt。
- [ ] 393 × 852 与 375 × 812 均无文字截断。
- [ ] 键盘、Safe Area、Home Indicator 和滚动状态正确。
- [ ] 关键颜色与文字通过对比度检查。
- [ ] 状态不只依靠颜色表达。

### 23.5 合规

- [ ] 健康数据用途和删除方式可达。
- [ ] 权限请求可选并说明价值。
- [ ] 订阅价格、周期、续费、取消与恢复购买清晰。
- [ ] 买断课与历史数据不被付费墙误锁。
- [ ] 医疗边界和敏感操作文案已审查。

---

## 24. 待确认事项

| ID | 待确认 | 当前原型基线 | 影响 |
|---|---|---|---|
| M-Q01 | 免费体验 1 天或 7 天 | 7 天 | 今日、付费墙、消息 |
| M-Q02 | 季卡/年卡与价格 | 仅月卡可售，其余不展示为可买 | 付费墙 |
| M-Q03 | 老用户折扣或积分权益 | 首月折扣仅作占位 | 老用户转化 |
| M-Q04 | 原 App 能力复用范围 | 课程库/社区按 P1 | 开发量 |
| M-Q05 | 小程序数据字段与质量 | 三类资产抽象 | 迁移 |
| M-Q06 | 周期阶段与安全规则审核方 | 后台审核话术与规则 | 健康风险 |
| M-Q07 | 完课、打卡与连胜最终规则 | 80%、多课全完成、不补打 | 播放与奖励 |
| M-Q08 | 积分商城是否 MVP 履约 | 仅余额与明细 | 商城链路 |
| M-Q09 | 国内渠道、支付与许可证 | iOS 默认 Apple IAP | 上架与支付 |
| M-Q10 | 防盗录范围 | 鉴权链接、时效、异常记录 | 播放器成本 |

这些事项确认后，应先更新 `src/data/mobile/decisions.ts` 和本文件，再更新提示词与视觉稿。

---

## 25. 资产清单

进入高保真前应准备：

- [ ] Logo 横版、竖版、图形标和安全区规范。
- [ ] iOS App Icon。
- [ ] 中文字体授权与字体文件。
- [ ] Jo 教练头像与人物摄影。
- [ ] 课程封面照片及课程—图片映射。
- [ ] 欢迎页品牌影像或插画。
- [ ] 分享海报品牌元素。
- [ ] 统一图标库。
- [ ] 课程阶段、适用/不适用、安全提示的审核文案。
- [ ] 隐私、健康数据和订阅正式协议文本。

资产未到位时使用明确占位，不将 AI 生成素材混入正式组件库。

---

## 26. 关联文件

- `docs/stitch-prompts-mobile-ins.md`：现有 Stitch 主提示词包，作为历史输入参考。
- `docs/stitch-prompts-mobile-ins-supplement.md`：状态补充包。
- `docs/stitch-prompt-system-human-mobile-v2.md`：去 AI 味的执行提示词工程。
- `src/data/mobile/screens-a.ts`：新用户主链路线框数据。
- `src/data/mobile/screens-b.ts`：迁移与每日调整线框数据。
- `src/data/mobile/screens-c.ts`：订阅、Tab 与探索线框数据。
- `src/data/mobile/decisions.ts`：B-01…B-13 与 M-Q01…M-Q10。

---

## 27. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-07 | 建立移动端设计单一事实源；整合产品规则、视觉系统、组件、页面、状态、AI 治理与验收规范 |

